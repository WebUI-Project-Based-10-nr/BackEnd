components:
  schemas:
    Subject:
      type: object
      properties:
        _id:
          type: string
        name:
          type: string
        category:
          $ref: '#/components/schemas/Category'
